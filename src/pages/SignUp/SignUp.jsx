import React from 'react'
import { auth } from '../../../Database/Firebase.config'

const SignUp = () => {
  console.log(auth);
  return (
    <div>
      SignUp
    </div>
  )
}

export default SignUp
